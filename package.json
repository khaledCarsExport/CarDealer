{
  "name": "khaled-cars-website",
  "version": "1.0.0",
  "description": "Professional car dealer website with persistent storage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "backup": "node -e \"const fs=require('fs'); const path=require('path'); const backupFile=path.join('data', 'cars-backup-' + Date.now() + '.json'); fs.copyFileSync('data/cars.json', backupFile); console.log('Backup created:', backupFile);\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}